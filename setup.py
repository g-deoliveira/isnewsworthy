from setuptools import setup

setup(
    name='newsworthy',
    version='0.0.1',
    description='Utilities for getting website data, parsing the source and training ML models',
    packages=['newsworthy'],
    package_dir={"":"src"},
)